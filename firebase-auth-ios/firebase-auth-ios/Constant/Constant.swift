import Foundation
import SwiftUI

struct Constant {
    static let bundleIdentifier = "co.teraglobal.patrons.operatorFramework"
    static let UIScreenWidth = UIScreen.main.bounds.width
    static let UIScreenHeight = UIScreen.main.bounds.height
    static let selectedLanguageKey = "selectedLanguageIndex"
    static let jwtTokenUserDefaultKey = "decodedJWTClaims"
}
